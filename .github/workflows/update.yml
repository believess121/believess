name: 🔄 构建并部署软件源 (GitHub Actions源)

on:
  push:
    paths:
      - 'debs/**'
  workflow_dispatch:
    inputs:
      force_rebuild:
        description: '强制重建所有索引'
        required: false
        default: 'false'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: 📊 检查deb文件
      run: |
        echo "=== 检查debs文件夹 ==="
        if [ ! -d "debs" ]; then
          echo "❌ debs文件夹不存在"
          mkdir -p debs
        fi
        
        echo "📁 文件夹内容："
        ls -la debs/ || echo "空文件夹"
        
        echo "🔍 查找所有.deb文件："
        find debs -name "*.deb" -type f | while read deb; do
          echo "  - $deb"
          ls -lh "$deb"
        done
        
        COUNT=$(find debs -name "*.deb" -type f | wc -l)
        echo "✅ 总共找到 $COUNT 个.deb文件"
        
        if [ $COUNT -eq 0 ]; then
          echo "⚠️ 警告：没有找到.deb文件"
        fi
    
    - name: 🔧 生成包含所有版本的Packages文件
      run: |
        echo "=== 生成包含所有版本的Packages文件 ==="
        
        # 为Pages部署创建专门目录
        rm -rf ./_pages_build
        mkdir -p ./_pages_build
        
        # 如果没有deb文件，创建空的Packages
        if [ ! -d "debs" ] || [ -z "$(find debs -name '*.deb' -print -quit)" ]; then
          echo "# Empty repository - waiting for packages" > ./_pages_build/Packages
          echo "没有找到.deb文件，创建空索引"
        else
          echo "# Believess Repository" > ./_pages_build/Packages
          echo "# Generated: $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> ./_pages_build/Packages
          echo "# This file includes ALL versions of all packages" >> ./_pages_build/Packages
          echo "" >> ./_pages_build/Packages
          
          # 使用dpkg-scanpackages处理所有文件
          cd debs
          
          # 为每个deb文件单独处理，确保所有版本都被包含
          for DEB in *.deb; do
            if [ -f "$DEB" ]; then
              echo "🔨 处理: $DEB"
              
              # 提取包名用于临时处理
              PKG_NAME=$(echo "$DEB" | cut -d_ -f1)
              
              # 创建一个临时目录来处理这个deb
              TEMP_DIR="/tmp/deb_$PKG_NAME"
              mkdir -p "$TEMP_DIR"
              cp "$DEB" "$TEMP_DIR/"
              
              # 生成这个deb的条目
              dpkg-scanpackages "$TEMP_DIR" /dev/null 2>/dev/null | \
                sed "s|Filename: $TEMP_DIR/|Filename: ./debs/|" >> ../_pages_build/Packages
              
              # 添加空行分隔
              echo "" >> ../_pages_build/Packages
              
              rm -rf "$TEMP_DIR"
            fi
          done
          
          cd ..
          
          # 手动验证
          echo "📊 生成的条目统计："
          echo "总条目数: $(grep -c '^Package:' ./_pages_build/Packages)"
          echo "唯一包名: $(grep '^Package:' ./_pages_build/Packages | sort -u | wc -l)"
        fi
    
    - name: 🗜️ 创建压缩版本
      run: |
        echo "=== 创建压缩文件 ==="
        
        if [ -f "./_pages_build/Packages" ]; then
          cd ./_pages_build
          
          echo "📄 Packages文件前100个字符："
          head -c 100 Packages
          echo ""
          
          echo "📦 创建压缩版本..."
          bzip2 -9fk Packages
          gzip -9fk Packages
          
          echo "📊 文件大小："
          ls -lh Packages*
          
          cd ..
        else
          echo "❌ Packages文件不存在"
          cd ./_pages_build
          touch Packages.bz2 Packages.gz
          cd ..
        fi
    
    - name: 📝 创建调试信息
      run: |
        echo "=== 调试信息 ===" > ./_pages_build/DEBUG.md
        echo "生成时间: $(date)" >> ./_pages_build/DEBUG.md
        echo "部署模式: GitHub Actions Pages" >> ./_pages_build/DEBUG.md
        echo "" >> ./_pages_build/DEBUG.md
        
        echo "**debs文件夹内容：**" >> ./_pages_build/DEBUG.md
        echo '```' >> ./_pages_build/DEBUG.md
        ls -la debs/ >> ./_pages_build/DEBUG.md 2>/dev/null || echo "空文件夹" >> ./_pages_build/DEBUG.md
        echo '```' >> ./_pages_build/DEBUG.md
        echo "" >> ./_pages_build/DEBUG.md
        
        if [ -f "./_pages_build/Packages" ]; then
          echo "**Packages文件统计：**" >> ./_pages_build/DEBUG.md
          echo "- 总行数: $(wc -l < ./_pages_build/Packages)" >> ./_pages_build/DEBUG.md
          echo "- 包条目: $(grep -c '^Package:' ./_pages_build/Packages)" >> ./_pages_build/DEBUG.md
          echo "- 版本数: $(grep -c '^Version:' ./_pages_build/Packages)" >> ./_pages_build/DEBUG.md
          echo "" >> ./_pages_build/DEBUG.md
          echo "**所有包和版本：**" >> ./_pages_build/DEBUG.md
          echo '```' >> ./_pages_build/DEBUG.md
          awk '/^Package:/ {pkg=$2} /^Version:/ {ver=$2} /^$/ {if(pkg && ver) printf "%-30s %s\n", pkg, ver; pkg=""; ver=""}' ./_pages_build/Packages | \
            sort >> ./_pages_build/DEBUG.md
          echo '```' >> ./_pages_build/DEBUG.md
        else
          echo "Packages文件未生成" >> ./_pages_build/DEBUG.md
        fi
        
        echo "" >> ./_pages_build/DEBUG.md
        echo "**构建目录内容：**" >> ./_pages_build/DEBUG.md
        echo '```' >> ./_pages_build/DEBUG.md
        ls -la ./_pages_build/ >> ./_pages_build/DEBUG.md
        echo '```' >> ./_pages_build/DEBUG.md
    
    - name: 🏗️ 配置GitHub Pages
      uses: actions/configure-pages@v4
    
    - name: 📤 上传构建产物
      uses: actions/upload-pages-artifact@v3
      with:
        path: './_pages_build'
    
    - name: 🚀 部署到GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
      
    - name: 📢 部署完成通知
      run: |
        echo "=== 部署完成 ==="
        echo "✅ 软件源已成功部署到 GitHub Pages!"
        echo ""
        echo "📦 部署的包数量: $(grep -c '^Package:' ./_pages_build/Packages 2>/dev/null || echo '0')"
        echo "🌐 访问地址: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
        echo "📄 Packages文件: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/Packages"
        echo ""
        echo "🎯 在Sileo中添加源地址:"
        echo "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
        echo ""
        echo "🔄 下次更新只需上传.deb文件到debs/目录即可自动部署"
